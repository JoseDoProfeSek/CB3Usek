using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShootBall : MonoBehaviour
{
    //Rigidbody rb;
    //public float speed = 5;
    //public float rootSpeed = 10;

    //Animator anim;

    //public GameObject[] arregloDire; 
    //// Start is called before the first frame update
    //void Start()
    //{
    //    rb = GetComponent<Rigidbody>();
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Space))
    //    {
    //        rb.AddForce(transform.up * speed, ForceMode.Impulse);
    //    }

    //    if (Input.GetKey(KeyCode.A))
    //    {
    //        transform.Rotate(Vector3.forward * rootSpeed * Time.deltaTime);
       
    //    }
    //    if (Input.GetKey(KeyCode.D))
    //    {
    //        transform.Rotate(Vector3.forward * -rootSpeed * Time.deltaTime);
           
    //    }
    //    Trayectoria();
    //    anim.SetFloat("mifloat", 0);
    //    anim.SetTrigger();
    //    anim.SetBool();
    //}

    //private void Trayectoria()
    //{
    //    for (int i = 0; i < arregloDire.Length; i++)
    //    {
    //        arregloDire[i].transform.position = transform.position + (transform.up * speed) * i; 
    //    }
    //}

    
}
